step 1)
    npm init (it will creat package.json file)

// Once package.json is generated, use the following command
// to register yourself with NPM repository site using a valid email address.
step 2)
    npm adduser
    username: klsadlf
    password:
    Email : (this is public)

//It is time now to publish your module −
step 3)
    npm publish
    // If everything is fine with your module,
    // then it will be published in the repository and
    // will be accessible to install using NPM like any other Node.js module. (e.g. express)
