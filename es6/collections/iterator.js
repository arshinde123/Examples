//Iterator is an object which allows to access a collection of objects one at a time.
//Both set and map have methods which returns an iterator.

//iterators are objects with next() method.
//When next() method is invoked,
//it returns an object with 'value' and 'done' properties . 'done' is boolean,
//this will return true after reading all items in the collection

//Methods of iterator
// 1 entries()
// 2 keys()
// 3 values()
